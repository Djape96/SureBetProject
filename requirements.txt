# Core dependencies for SureBetProject automation & scraping
requests
python-dotenv
selenium>=4.15.0
webdriver-manager>=4.0.0
beautifulsoup4
fastapi  # used by API app (optional for scheduled script)
# Optional runtime for FastAPI server (add if you serve the API):
uvicorn[standard]
